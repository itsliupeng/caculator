# A caculator demo

## simple version

```ruby
loop do
    puts "> #{eval($stdin.gets)}"
end
```

## how to use

int root directory, `ruby sctipts/caculator_demo.rb`

## test

in root directory, `rspec spec`


